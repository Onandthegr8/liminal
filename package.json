{
  "name": "liminal",
  "version": "1.0.0",
  "description": "Backrooms x Granny first-person survival horror — one web build for desktop and mobile.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
